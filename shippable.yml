# language setting
language: node_js

# version numbers, testing against one version of node
node_js:
    - 0.10.33


build:
   pre_ci_boot:
        image_name: elgalu/selenium
        image_tag: 2.53.0o
        pull: true

   ci:
       - grunt
